---
'@perimetre/ui': minor
---

Add `ImageSequence360` component — a 360° product spin viewer built on `@mediamonks/fast-image-sequence` and `@use-gesture/react`. Supports mouse drag, touch, and touchpad with axis-locked horizontal gestures, and resizes responsively with its container.
