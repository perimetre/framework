---
'@perimetre/eslint-config-base': patch
---

Fix TypeScript parser configuration to support JSON files and config files. Added `extraFileExtensions` for `.json` files and `allowDefaultProject` for config files like `*.config.mjs`, `*.config.js`, and `*.config.ts`.
