/** @type {import('tailwindcss').Config} */
export default {
  // Scope all utilities to only work inside [data-pui-brand]
  important: '[data-pui-brand]'
  // Avoid adding extra settings here and prefer options in css
};
