import { cva } from '@/lib/cva';

/**
 * Microbird Commercial brand SectionHorizontalHeader variant overrides
 * Only overrides font-family; composes with Acorn base variant
 */
export const sectionHorizontalHeaderMicrobirdCommercialVariants = cva({
  base: ['pui:font-pui-sans']
});
