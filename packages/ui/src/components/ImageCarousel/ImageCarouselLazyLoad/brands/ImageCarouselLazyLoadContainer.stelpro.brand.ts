import { cva } from '@/lib/cva';

/**
 * Stelpro brand ImageCarouselLazyLoadContainer variant — sharp corners to match
 * the squared-off PDP gallery frame.
 */
export const imageCarouselLazyLoadContainerStelproVariants = cva({
  base: ['pui:rounded-none']
});
