import { cva } from '@/lib/cva';

/**
 * MicroBird Commercial brand Accordion variants.
 *
 * Most theming flows through semantic tokens (typo-accordion-*,
 * border-pui-border-accordion-*, text colors). Add structural overrides here
 * only when the brand needs a layout change beyond what tokens express.
 */
export const accordionMicroBirdCommercialVariants = cva({ base: '' });

export const accordionTriggerMicroBirdCommercialVariants = cva({ base: '' });

export const accordionHeadingMicroBirdCommercialVariants = cva({ base: '' });

export const accordionEyebrowMicroBirdCommercialVariants = cva({ base: '' });

export const accordionTitleMicroBirdCommercialVariants = cva({ base: '' });

export const accordionIconMicroBirdCommercialVariants = cva({ base: '' });

export const accordionIconBarMicroBirdCommercialVariants = cva({ base: '' });

export const accordionContentMicroBirdCommercialVariants = cva({ base: '' });

export const accordionContentInnerMicroBirdCommercialVariants = cva({
  base: ''
});
