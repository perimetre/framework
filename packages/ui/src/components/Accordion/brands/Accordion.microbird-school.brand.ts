import { cva } from '@/lib/cva';

/**
 * MicroBird School brand Accordion variants.
 *
 * Most theming flows through semantic tokens (typo-accordion-*,
 * border-pui-border-accordion-*, text colors). Add structural overrides here
 * only when the brand needs a layout change beyond what tokens express.
 */
export const accordionMicroBirdSchoolVariants = cva({ base: '' });

export const accordionTriggerMicroBirdSchoolVariants = cva({ base: '' });

export const accordionHeadingMicroBirdSchoolVariants = cva({ base: '' });

export const accordionEyebrowMicroBirdSchoolVariants = cva({ base: '' });

export const accordionTitleMicroBirdSchoolVariants = cva({ base: '' });

export const accordionIconMicroBirdSchoolVariants = cva({ base: '' });

export const accordionIconBarMicroBirdSchoolVariants = cva({ base: '' });

export const accordionContentMicroBirdSchoolVariants = cva({ base: '' });

export const accordionContentInnerMicroBirdSchoolVariants = cva({ base: '' });
