import { cva } from '@/lib/cva';

/**
 * Sprig brand select variants
 *
 * Visual style is driven by semantic tokens in sprig/styles.css
 * (--pui-color-input-bg, --pui-color-input-border, --pui-radius-input, etc.).
 * No JS overrides unless there are structural differences.
 */
export const fieldBaseSelectSprigVariants = cva({
  base: []
});
