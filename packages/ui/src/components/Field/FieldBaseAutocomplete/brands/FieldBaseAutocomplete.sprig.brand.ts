import { cva } from '@/lib/cva';

/**
 * Sprig brand autocomplete variants
 *
 * Visual style is driven by semantic tokens in sprig/styles.css.
 * No JS overrides unless there are structural differences.
 */
export const fieldBaseAutocompleteInputSprigVariants = cva({ base: [] });
export const fieldBaseAutocompleteOptionsSprigVariants = cva({ base: [] });
export const fieldBaseAutocompleteOptionSprigVariants = cva({ base: [] });
