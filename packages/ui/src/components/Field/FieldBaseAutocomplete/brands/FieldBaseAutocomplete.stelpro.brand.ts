import { cva } from '@/lib/cva';

/**
 * Stelpro brand autocomplete variants
 *
 * Visual style is driven by semantic tokens in stelpro/styles.css.
 * No JS overrides unless there are structural differences.
 */
export const fieldBaseAutocompleteInputStelproVariants = cva({ base: [] });
export const fieldBaseAutocompleteOptionsStelproVariants = cva({ base: [] });
export const fieldBaseAutocompleteOptionStelproVariants = cva({ base: [] });
